// export const addToBooked = (dispatch, booked, user, meeting, active) => {
//   booked.push({ user, meeting, service: active, uid: shortid.generate() });
//   dispatch({ type: "UPDATE_BOOKED", payload: booked });
// };
