// export const selectPaymentType = (dispatch, method) => {
//   const data = { ...method, title: `Payment type: ${method.name}` };
//   dispatch({ type: "SELECT_PAYMENT_TYPE", payload: data });
// };
